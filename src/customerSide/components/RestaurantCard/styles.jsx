import { Grid } from '@mui/material';
import styled from 'styled-components';

const MainContainer = styled(Grid)`
    border-radius: 20px;
    padding: 10px;
`
const RestaurantImage = styled.img`
    border-radius: 20px;
`

export { MainContainer, RestaurantImage }