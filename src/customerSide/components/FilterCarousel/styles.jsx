import styled from 'styled-components';

const CarouselContainer = styled.div`
    display: flex;
    flexDirection: row;
    flexWrap: nowrap;
    gap: 50px;
    padding: 10px;
    overflowX: auto;
`

export { CarouselContainer }